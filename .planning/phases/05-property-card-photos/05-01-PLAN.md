---
phase: 05-property-card-photos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - homesearch/providers/homeharvest_provider.py
  - homesearch/providers/redfin_provider.py
  - frontend/src/components/PropertyCard.jsx
autonomous: false
requirements:
  - PHOTO-01
  - PHOTO-02
must_haves:
  truths:
    - "Property cards with photos show real listing thumbnail images without 403 errors"
    - "Property cards without photos show a styled Home icon and No Photo Available label"
    - "Placeholder pulses while a photo is loading, then stops on load or error"
    - "Listings missing primary_photo but having alt_photos still display a thumbnail"
  artifacts:
    - path: "frontend/src/components/PropertyCard.jsx"
      provides: "Photo rendering with referrerPolicy fix, pulse loading state, styled placeholder"
      contains: "referrerPolicy"
    - path: "homesearch/providers/homeharvest_provider.py"
      provides: "alt_photos fallback when primary_photo is null"
      contains: "alt_photos"
  key_links:
    - from: "homesearch/providers/homeharvest_provider.py"
      to: "homesearch/models.py"
      via: "Listing(photo_url=photo)"
      pattern: "photo_url=photo"
    - from: "frontend/src/components/PropertyCard.jsx"
      to: "CDN image URL"
      via: "img src with referrerPolicy no-referrer"
      pattern: "referrerPolicy"
---

<objective>
Diagnose the photo pipeline, fix CDN hotlink blocking, add alt_photos fallback in the homeharvest provider, and polish the no-photo placeholder with a Home icon and loading pulse animation.

Purpose: Users see real listing photos in the web dashboard instead of broken images or grey rectangles. The diagnose-first protocol (D-02, D-05) ensures we confirm root causes before writing code.
Output: Working photo thumbnails on property cards, styled fallback placeholder, Redfin photo key diagnostic output logged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-property-card-photos/05-CONTEXT.md
@.planning/phases/05-property-card-photos/05-RESEARCH.md
@.planning/phases/05-property-card-photos/05-UI-SPEC.md

<interfaces>
From homesearch/models.py:
  class Listing(BaseModel): photo_url: str = ""

From homesearch/providers/homeharvest_provider.py line 107:
  photo = str(row.get("primary_photo", "") or row.get("img_src", "") or "")

From frontend/src/components/PropertyCard.jsx line 1:
  import { ExternalLink, Bed, Bath, Ruler, Calendar } from 'lucide-react'
  -- No React import. No useState. No Home icon import.

Current photo block (lines 27-42) uses:
  - No referrerPolicy attribute on img
  - onError handler: e.target.style.display = 'none'; e.target.nextSibling.style.display = 'flex'
  - Placeholder: bare text "No Photo Available" with text-sm, hidden class when photo_url truthy
  - img and placeholder div are adjacent siblings (nextSibling is correct)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose photo pipeline and confirm column names and Redfin photo key shape</name>
  <files>homesearch/providers/homeharvest_provider.py, homesearch/providers/redfin_provider.py</files>
  <read_first>
    - homesearch/providers/homeharvest_provider.py (current photo extraction at line 107)
    - homesearch/providers/redfin_provider.py (current multi-shape photo handler at lines 112-117)
    - homesearch/models.py (Listing.photo_url field definition)
  </read_first>
  <action>
Per D-02 and D-05 -- diagnose before writing any production code changes.

Step 1: Add temporary diagnostic logging to homeharvest provider.
In homesearch/providers/homeharvest_provider.py, inside search(), after line 44 (the df.empty check), add two print statements:

    print("[HH DEBUG] photo columns:", [c for c in df.columns if "photo" in c.lower()])
    print("[HH DEBUG] sample primary_photo:", df["primary_photo"].dropna().head(3).tolist() if "primary_photo" in df.columns else "COLUMN MISSING")

Step 2: Add temporary diagnostic logging to Redfin provider.
In homesearch/providers/redfin_provider.py, inside _home_to_listing(), immediately after line 87 (home_data = home.get("homeData", home)), add:

    photos_raw = home_data.get("photos")
    print(f"[Redfin DEBUG] photos type={type(photos_raw).__name__}, first_keys={list(photos_raw[0].keys()) if isinstance(photos_raw, list) and photos_raw and isinstance(photos_raw[0], dict) else repr(photos_raw)[:80]}")

Step 3: Run a real search to populate SQLite and trigger diagnostic output.
Use the Python API directly (CLI requires interactive input):

    python -c "
    from homesearch.services.search_service import SearchService
    from homesearch.models import SearchCriteria
    svc = SearchService()
    results = svc.run_search(SearchCriteria(location='10001'))
    print(f'Total results: {len(results)}')
    for r in results[:3]:
        print(f'  source={r.source}, photo_url={r.photo_url[:80] if r.photo_url else None}')
    "

Step 4: Check SQLite for non-null photo_url values:

    sqlite3 ~/.homesearch/homesearch.db "SELECT source, photo_url FROM listings WHERE photo_url != '' LIMIT 5;"

Step 5: Remove ALL temporary diagnostic logging from BOTH provider files. The print statements from Steps 1 and 2 are diagnostic only and must NOT remain in production code.

Step 6: Record findings in the SUMMARY -- column names confirmed or drifted, Redfin key shape, photo_url coverage count.
  </action>
  <verify>
    <automated>sqlite3 ~/.homesearch/homesearch.db "SELECT COUNT(*) FROM listings WHERE photo_url != '';" 2>/dev/null || echo "No DB or listings yet"</automated>
  </verify>
  <acceptance_criteria>
    - Terminal output contains "[HH DEBUG] photo columns:" with a list including "primary_photo"
    - Terminal output contains "[Redfin DEBUG] photos type=" with key information
    - SQLite query "SELECT COUNT(*) FROM listings WHERE photo_url != ''" returns a number greater than 0
    - homesearch/providers/homeharvest_provider.py does NOT contain "[HH DEBUG]" after cleanup
    - homesearch/providers/redfin_provider.py does NOT contain "[Redfin DEBUG]" after cleanup
  </acceptance_criteria>
  <done>Photo pipeline diagnosed: column names confirmed, Redfin photo key shape logged, SQLite photo_url coverage verified, all diagnostic logging removed</done>
</task>

<task type="auto">
  <name>Task 2: Add alt_photos fallback in homeharvest provider and update PropertyCard with referrerPolicy fix and placeholder polish</name>
  <files>homesearch/providers/homeharvest_provider.py, frontend/src/components/PropertyCard.jsx</files>
  <read_first>
    - homesearch/providers/homeharvest_provider.py (line 107 -- current photo extraction in _row_to_listing)
    - frontend/src/components/PropertyCard.jsx (full file -- current imports, photo block lines 27-42)
    - .planning/phases/05-property-card-photos/05-UI-SPEC.md (complete placeholder spec, state machine, exact class strings)
    - .planning/phases/05-property-card-photos/05-RESEARCH.md (Pattern 3: alt_photos is comma-joined string)
  </read_first>
  <action>
Two files, two independent edits. Per D-01, D-04, D-06 through D-10.

EDIT A: homesearch/providers/homeharvest_provider.py -- alt_photos fallback (per D-04)

In _row_to_listing(), after line 107 (photo = str(row.get("primary_photo", "") or row.get("img_src", "") or "")), add these 4 lines before the "# Garage / basement detection" comment:

    if not photo:
        alt = str(row.get("alt_photos", "") or "")
        if alt:
            photo = alt.split(", ")[0]

CRITICAL: alt_photos in the DataFrame is a COMMA-JOINED STRING like "https://a.com/1.jpg, https://b.com/2.jpg". Use .split(", ")[0] to get the first URL. Do NOT use alt_photos[0] which would return a single character.

Do NOT touch redfin_provider.py for alt_photos (deferred per CONTEXT.md).

EDIT B: frontend/src/components/PropertyCard.jsx -- referrerPolicy + placeholder polish

B1. Add React useState import as a NEW first line of the file:
    import { useState } from 'react'

B2. Update the lucide-react import (line 1, which becomes line 2 after B1) to add Home:
    import { ExternalLink, Bed, Bath, Ruler, Calendar, Home } from 'lucide-react'

B3. Inside the component function body, after the destructuring block and before the priceStr line, add:
    const [imgLoaded, setImgLoaded] = useState(false)

B4. Add the placeholderClass computation after imgLoaded:
    const placeholderClass = `w-full h-52 bg-slate-100 flex flex-col items-center justify-center text-slate-400 gap-2${photo_url && !imgLoaded ? ' animate-pulse' : ''}`

B5. Replace the entire photo block (the <a> tag from line 27 through line 42) with:

    <a href={source_url || '#'} target="_blank" rel="noopener noreferrer" className="relative block">
      {photo_url ? (
        <img
          src={photo_url}
          alt={address}
          className="w-full h-52 object-cover bg-slate-100"
          referrerPolicy="no-referrer"
          onLoad={() => setImgLoaded(true)}
          onError={(e) => {
            e.target.style.display = 'none'
            e.target.nextSibling.style.display = 'flex'
            setImgLoaded(true)
          }}
        />
      ) : null}
      <div
        className={placeholderClass}
        style={photo_url && imgLoaded ? { display: 'none' } : undefined}
      >
        <Home size={28} />
        <span className="text-xs">No Photo Available</span>
      </div>
      <Badge variant="secondary" className="absolute top-2 right-2 capitalize shadow-sm">
        {source}
      </Badge>
    </a>

Key requirements enforced:
- referrerPolicy="no-referrer" on img (D-01) -- suppresses Referer header for CDN hotlink bypass
- onLoad sets imgLoaded=true to stop pulse and hide placeholder (D-10)
- onError keeps existing DOM manipulation AND sets imgLoaded=true to stop pulse (D-09)
- img and placeholder div remain ADJACENT SIBLINGS -- onError uses e.target.nextSibling (no elements between them)
- Placeholder uses Home icon size={28} + "No Photo Available" span with text-xs (D-06, D-07)
- animate-pulse only when photo_url is truthy AND imgLoaded is false -- no eternal pulse on no-photo cards (D-10)
- h-52 preserved on placeholder div (D-08)
- Background bg-slate-100, text text-slate-400 (D-07)
  </action>
  <verify>
    <automated>cd /Users/iamtron/Documents/GitHub/HomerFindr && python -c "
import inspect
from homesearch.providers.homeharvest_provider import HomeHarvestProvider
src = inspect.getsource(HomeHarvestProvider._row_to_listing)
assert 'alt_photos' in src, 'FAIL: alt_photos fallback missing'
assert '.split' in src, 'FAIL: split on comma missing'
print('Python OK: alt_photos fallback with .split present')
" && grep -c 'referrerPolicy' frontend/src/components/PropertyCard.jsx && grep -c 'useState' frontend/src/components/PropertyCard.jsx && grep -c 'Home' frontend/src/components/PropertyCard.jsx && cd frontend && npx --yes vite build --mode production 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - homesearch/providers/homeharvest_provider.py contains the string "alt_photos" in _row_to_listing
    - homesearch/providers/homeharvest_provider.py contains ".split" near "alt_photos"
    - frontend/src/components/PropertyCard.jsx line 1 is: import { useState } from 'react'
    - frontend/src/components/PropertyCard.jsx contains: referrerPolicy="no-referrer"
    - frontend/src/components/PropertyCard.jsx contains: Home (in lucide-react import AND in JSX)
    - frontend/src/components/PropertyCard.jsx contains: setImgLoaded(true) (appears in both onLoad and onError)
    - frontend/src/components/PropertyCard.jsx contains: animate-pulse (conditional, not hardcoded)
    - frontend/src/components/PropertyCard.jsx contains: text-xs (on the No Photo Available span)
    - img element and placeholder div remain adjacent siblings (no elements between them)
    - Vite build completes without errors (exit code 0)
  </acceptance_criteria>
  <done>alt_photos fallback added in homeharvest provider using .split(", ")[0] pattern. PropertyCard updated with referrerPolicy="no-referrer", useState for imgLoaded, Home icon placeholder with animate-pulse loading state, and text-xs label. Vite build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify photos render in web dashboard</name>
  <files>none</files>
  <action>
Human visual verification of the complete photo pipeline fix. Start the dev server and visually confirm:

1. Start the server: cd /Users/iamtron/Documents/GitHub/HomerFindr and run python -m homesearch serve
2. Open http://localhost:8000 in your browser
3. Run a search (any location)
4. Check property cards in the results grid:
   a. Cards WITH photos: thumbnail image loads without 403 errors (check DevTools Network tab)
   b. Cards WITHOUT photos: centered Home icon (~28px) with "No Photo Available" text below, light grey bg
   c. While loading: placeholder briefly pulses then stops once image loads
5. Verify no broken image icons appear anywhere in the grid
  </action>
  <verify>User visual confirmation</verify>
  <done>User confirms photos load correctly in property cards and placeholders display as designed</done>
</task>

</tasks>

<verification>
Phase verification checks:
1. grep referrerPolicy frontend/src/components/PropertyCard.jsx -- returns match
2. grep alt_photos homesearch/providers/homeharvest_provider.py -- returns match
3. grep useState frontend/src/components/PropertyCard.jsx -- returns match
4. grep "Home" frontend/src/components/PropertyCard.jsx -- returns match for import and JSX usage
5. cd frontend and npx vite build -- exits 0
6. No "[HH DEBUG]" or "[Redfin DEBUG]" strings remain in provider files
</verification>

<success_criteria>
- Real listing photos render in property cards without 403 errors (PHOTO-01)
- No-photo cards show Home icon + "No Photo Available" label on bg-slate-100 (PHOTO-02)
- Placeholder pulses during image load, stops on success or error
- Listings with null primary_photo but populated alt_photos still show a thumbnail
- Vite production build passes
- Human visual verification approved
</success_criteria>

<output>
After completion, create .planning/phases/05-property-card-photos/05-01-SUMMARY.md
</output>
