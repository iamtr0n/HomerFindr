---
phase: quick
plan: 260326-j1t
type: execute
wave: 1
depends_on: []
files_modified:
  - homesearch/providers/homeharvest_provider.py
autonomous: true
must_haves:
  truths:
    - "Selecting Coming Soon listing type does not crash with InvalidListingType"
    - "Coming Soon searches return pending/pre-market listings from homeharvest"
    - "Multi-type searches (e.g. For Sale + Coming Soon) execute in a single API call per location instead of one call per type"
  artifacts:
    - path: "homesearch/providers/homeharvest_provider.py"
      provides: "Fixed _LISTING_TYPE_MAP and batched listing_type calls"
      contains: "pending"
  key_links:
    - from: "homesearch/providers/homeharvest_provider.py"
      to: "homeharvest.scrape_property"
      via: "_LISTING_TYPE_MAP lookup and listing_type parameter"
      pattern: "listing_type.*pending"
---

<objective>
Fix the InvalidListingType crash when searching with Coming Soon and optimize multi-type searches to batch listing types into a single homeharvest API call per location.

Purpose: Coming Soon searches currently crash because homeharvest has no "coming_soon" listing type. Additionally, searching multiple listing types makes N separate API calls per ZIP when homeharvest natively accepts a list.
Output: Working Coming Soon searches mapped to "pending", fewer API calls for multi-type searches.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@homesearch/providers/homeharvest_provider.py
@homesearch/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix coming_soon mapping and batch listing types per location</name>
  <files>homesearch/providers/homeharvest_provider.py</files>
  <action>
Two changes in homeharvest_provider.py:

1. **Fix _LISTING_TYPE_MAP** (line 15): Change `ListingType.COMING_SOON: "coming_soon"` to `ListingType.COMING_SOON: "pending"`. The homeharvest library's valid ListingType enum values are: "for_sale", "for_rent", "pending", "sold". There is no "coming_soon" — it raises InvalidListingType.

2. **Batch listing types into a single scrape_property call per location**: In the `search()` method (lines 24-64), refactor the nested loop so that instead of iterating `for lt_enum in types_to_run` then `for location in locations` (making len(types) * len(locations) API calls), we:
   - Build the list of homeharvest type strings: `hh_types = [_LISTING_TYPE_MAP.get(lt, "for_sale") for lt in types_to_run]`
   - Deduplicate: `hh_types = list(dict.fromkeys(hh_types))` (preserves order)
   - Loop only over locations, passing the full list to `scrape_property(location=location, listing_type=hh_types, ...)` in a single call
   - For `past_days`: only pass `past_days=30` if "sold" is in `hh_types`
   - Update progress tracking: `total = len(locations)` (not locations * types)
   - In `_row_to_listing`, the `listing_type` parameter will still be a string for mapping to our internal type. Since batched results may contain mixed types, pass the first type from hh_types as default, OR detect from the row's status field if available. The simplest approach: pass `hh_types[0]` as the listing_type to `_row_to_listing` since homeharvest rows include a `status` column we can check. Actually, keep it simple: if only one type, pass it directly. If multiple types, pass "for_sale" as default — the listing_type on Listing is informational and the status column in homeharvest data can be checked later if needed.

Keep the 1.5s sleep per location (rate limiting). Keep the try/except per location with continue on error.
  </action>
  <verify>
    <automated>cd /Users/iamtron/Documents/GitHub/HomerFindr && python -c "
from homesearch.providers.homeharvest_provider import _LISTING_TYPE_MAP
from homesearch.models import ListingType
# Verify coming_soon maps to pending, not 'coming_soon'
assert _LISTING_TYPE_MAP[ListingType.COMING_SOON] == 'pending', f'Expected pending, got {_LISTING_TYPE_MAP[ListingType.COMING_SOON]}'
# Verify all mapped values are valid homeharvest types
valid = {'for_sale', 'for_rent', 'sold', 'pending'}
for k, v in _LISTING_TYPE_MAP.items():
    assert v in valid, f'{k} maps to invalid type {v}'
print('All assertions passed')
"</automated>
  </verify>
  <done>
    - _LISTING_TYPE_MAP maps COMING_SOON to "pending" (no more InvalidListingType crash)
    - Multi-type searches make 1 API call per location instead of N calls per location
    - All existing single-type searches continue to work unchanged
  </done>
</task>

</tasks>

<verification>
1. `python -c "from homesearch.providers.homeharvest_provider import _LISTING_TYPE_MAP; from homesearch.models import ListingType; assert _LISTING_TYPE_MAP[ListingType.COMING_SOON] == 'pending'"` passes
2. Provider module imports without errors
3. Search method signature unchanged (no downstream breakage)
</verification>

<success_criteria>
- Coming Soon listing type no longer raises InvalidListingType
- Multi-type searches are batched (1 API call per location, not N)
- No regressions in single-type search behavior
</success_criteria>

<output>
After completion, create `.planning/quick/260326-j1t-fix-coming-soon-invalidlistingtype-error/260326-j1t-SUMMARY.md`
</output>
