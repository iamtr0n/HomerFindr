---
phase: 03-web-ui-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tailwind.config.js
  - frontend/src/index.css
  - frontend/src/lib/utils.js
  - frontend/src/components/ui/Card.jsx
  - frontend/src/components/ui/Badge.jsx
  - frontend/src/components/ui/Button.jsx
  - frontend/src/App.jsx
autonomous: true
requirements: [WEB-06, XC-03]

must_haves:
  truths:
    - "The app uses a forest-green brand color palette instead of generic blue"
    - "The nav bar says HomerFindr with a house icon, not HomeSearch"
    - "Reusable Card, Badge, and Button primitives exist in frontend/src/components/ui/"
    - "A cn() utility function is available for conditional class merging"
  artifacts:
    - path: "frontend/tailwind.config.js"
      provides: "Brand color palette and Inter font"
      contains: "brand"
    - path: "frontend/src/lib/utils.js"
      provides: "cn() class merging utility"
      exports: ["cn"]
    - path: "frontend/src/components/ui/Card.jsx"
      provides: "Reusable Card, CardHeader, CardContent, CardFooter components"
      exports: ["Card", "CardHeader", "CardContent", "CardFooter"]
    - path: "frontend/src/components/ui/Badge.jsx"
      provides: "Badge component with variant support"
      exports: ["Badge"]
    - path: "frontend/src/components/ui/Button.jsx"
      provides: "Button component with variant and size props"
      exports: ["Button"]
    - path: "frontend/src/App.jsx"
      provides: "Updated nav with HomerFindr branding and green color scheme"
      contains: "HomerFindr"
  key_links:
    - from: "frontend/src/components/ui/Card.jsx"
      to: "frontend/src/lib/utils.js"
      via: "import { cn } from '../../lib/utils'"
      pattern: "import.*cn.*from.*utils"
    - from: "frontend/tailwind.config.js"
      to: "frontend/src/App.jsx"
      via: "brand-600 and brand-700 classes used in nav"
      pattern: "brand-"
---

<objective>
Establish the design system foundation: brand color palette, typography, shadcn/ui-style component primitives, and HomerFindr branding.

Purpose: All subsequent visual redesign plans (03, 04) depend on these shared primitives and color tokens. Building the design system first prevents inconsistency and duplication.
Output: Tailwind color config, cn() utility, Card/Badge/Button primitives, branded nav bar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-ui-redesign/03-RESEARCH.md

<interfaces>
<!-- Current state of files being modified -->

From frontend/tailwind.config.js (current — empty extend):
```javascript
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  theme: { extend: {} },
  plugins: [],
}
```

From frontend/src/App.jsx (current — blue nav, "HomeSearch" text):
```jsx
<nav className="bg-blue-600 text-white shadow-lg">
  <Link to="/" className="flex items-center gap-2 font-bold text-lg">
    <Home size={22} />
    HomeSearch
  </Link>
```

From frontend/src/index.css (current — system fonts):
```css
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}
```

Locked decision from STATE.md: "Use shadcn/ui for web component redesign (owned code, no runtime dependency)"
— Components are copy-pasted source, NOT installed via npx shadcn-ui init
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install helpers, configure Tailwind brand palette and typography</name>
  <files>frontend/package.json, frontend/tailwind.config.js, frontend/src/index.css, frontend/src/lib/utils.js</files>
  <read_first>
    - frontend/package.json (current dependencies)
    - frontend/tailwind.config.js (current config)
    - frontend/src/index.css (current global styles)
  </read_first>
  <action>
1. Install `clsx` and `tailwind-merge` for the cn() utility:
```bash
cd /Users/iamtron/Documents/GitHub/HomerFindr/frontend && npm install clsx tailwind-merge
```

2. Create `frontend/src/lib/utils.js`:
```javascript
import { clsx } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs) {
  return twMerge(clsx(inputs))
}
```

3. Update `frontend/tailwind.config.js` to:
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  theme: {
    extend: {
      colors: {
        brand: {
          50: '#f0fdf4',
          100: '#dcfce7',
          200: '#bbf7d0',
          300: '#86efac',
          400: '#4ade80',
          500: '#22c55e',
          600: '#16a34a',
          700: '#15803d',
          800: '#166534',
          900: '#14532d',
        },
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'BlinkMacSystemFont', 'Segoe UI', 'Roboto', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
```

4. Update `frontend/src/index.css` to import Inter font and set base styles:
```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  @apply bg-slate-50 text-slate-900 antialiased;
  font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}
```
  </action>
  <verify>
    <automated>cd /Users/iamtron/Documents/GitHub/HomerFindr/frontend && node -e "require('clsx'); require('tailwind-merge'); console.log('deps OK')" && grep -q "brand" tailwind.config.js && grep -q "Inter" tailwind.config.js && test -f src/lib/utils.js && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "brand" frontend/tailwind.config.js` returns color definitions including 50, 100, 200, 500, 600, 700, 800, 900
    - `grep "Inter" frontend/tailwind.config.js` shows Inter in fontFamily.sans
    - `cat frontend/src/lib/utils.js` shows cn function exporting twMerge(clsx(inputs))
    - `grep "clsx" frontend/package.json` shows clsx in dependencies
    - `grep "tailwind-merge" frontend/package.json` shows tailwind-merge in dependencies
    - `grep "Inter" frontend/src/index.css` shows the font import
    - `grep "bg-slate-50" frontend/src/index.css` shows the body background
  </acceptance_criteria>
  <done>Tailwind config has brand color palette (forest green) and Inter font, cn() utility exists, clsx + tailwind-merge installed, index.css updated with Inter import and slate-50 background</done>
</task>

<task type="auto">
  <name>Task 2: Create shadcn/ui primitives and update App.jsx branding</name>
  <files>frontend/src/components/ui/Card.jsx, frontend/src/components/ui/Badge.jsx, frontend/src/components/ui/Button.jsx, frontend/src/App.jsx</files>
  <read_first>
    - frontend/src/lib/utils.js (cn function just created in Task 1)
    - frontend/src/App.jsx (current nav with blue theme and "HomeSearch" text)
  </read_first>
  <action>
**Create shadcn/ui-style Card primitive** at `frontend/src/components/ui/Card.jsx`:

```jsx
import { cn } from '../../lib/utils'

export function Card({ className, ...props }) {
  return (
    <div
      className={cn('rounded-lg border border-slate-200 bg-white shadow-sm', className)}
      {...props}
    />
  )
}

export function CardHeader({ className, ...props }) {
  return <div className={cn('flex flex-col space-y-1.5 p-6', className)} {...props} />
}

export function CardContent({ className, ...props }) {
  return <div className={cn('p-6 pt-0', className)} {...props} />
}

export function CardFooter({ className, ...props }) {
  return <div className={cn('flex items-center p-6 pt-0', className)} {...props} />
}
```

**Create Badge primitive** at `frontend/src/components/ui/Badge.jsx`:

```jsx
import { cn } from '../../lib/utils'

const variants = {
  default: 'bg-brand-600 text-white',
  secondary: 'bg-slate-100 text-slate-800',
  success: 'bg-green-100 text-green-700',
  warning: 'bg-amber-100 text-amber-700',
  destructive: 'bg-red-100 text-red-700',
  outline: 'border border-slate-200 text-slate-700',
}

export function Badge({ variant = 'default', className, ...props }) {
  return (
    <span
      className={cn(
        'inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium',
        variants[variant],
        className
      )}
      {...props}
    />
  )
}
```

**Create Button primitive** at `frontend/src/components/ui/Button.jsx`:

```jsx
import { cn } from '../../lib/utils'

const variants = {
  default: 'bg-brand-600 text-white hover:bg-brand-700',
  secondary: 'bg-slate-100 text-slate-800 hover:bg-slate-200',
  outline: 'border border-slate-200 bg-white text-slate-700 hover:bg-slate-50',
  ghost: 'text-slate-700 hover:bg-slate-100',
  destructive: 'bg-red-600 text-white hover:bg-red-700',
}

const sizes = {
  sm: 'px-3 py-1.5 text-sm',
  md: 'px-4 py-2 text-sm',
  lg: 'px-6 py-2.5 text-base',
  icon: 'p-2',
}

export function Button({ variant = 'default', size = 'md', className, ...props }) {
  return (
    <button
      className={cn(
        'inline-flex items-center justify-center gap-2 rounded-lg font-medium transition-colors disabled:opacity-50 disabled:pointer-events-none',
        variants[variant],
        sizes[size],
        className
      )}
      {...props}
    />
  )
}
```

**Update App.jsx branding (XC-03):**

Replace the entire `frontend/src/App.jsx` with:

```jsx
import { BrowserRouter, Routes, Route, Link, useLocation } from 'react-router-dom'
import { Home, Search, PlusCircle } from 'lucide-react'
import Dashboard from './pages/Dashboard'
import NewSearch from './pages/NewSearch'
import SearchResults from './pages/SearchResults'

function Nav() {
  const location = useLocation()
  const isActive = (path) => location.pathname === path ? 'bg-brand-700' : 'hover:bg-brand-700/80'

  return (
    <nav className="bg-brand-600 text-white shadow-lg">
      <div className="max-w-7xl mx-auto px-4">
        <div className="flex items-center h-14 gap-6">
          <Link to="/" className="flex items-center gap-2 font-bold text-lg">
            <Home size={22} />
            HomerFindr
          </Link>
          <div className="flex gap-1 ml-6">
            <Link to="/" className={`px-3 py-1.5 rounded text-sm flex items-center gap-1.5 ${isActive('/')}`}>
              <Search size={16} /> Dashboard
            </Link>
            <Link to="/search/new" className={`px-3 py-1.5 rounded text-sm flex items-center gap-1.5 ${isActive('/search/new')}`}>
              <PlusCircle size={16} /> New Search
            </Link>
          </div>
        </div>
      </div>
    </nav>
  )
}

export default function App() {
  return (
    <BrowserRouter>
      <Nav />
      <main className="max-w-7xl mx-auto px-4 py-6">
        <Routes>
          <Route path="/" element={<Dashboard />} />
          <Route path="/search/new" element={<NewSearch />} />
          <Route path="/search/:id/results" element={<SearchResults />} />
        </Routes>
      </main>
    </BrowserRouter>
  )
}
```

Key changes: `bg-blue-600` -> `bg-brand-600`, `bg-blue-700` -> `bg-brand-700`, `hover:bg-blue-700` -> `hover:bg-brand-700/80`, "HomeSearch" -> "HomerFindr", removed unused `Settings` import from lucide-react.
  </action>
  <verify>
    <automated>cd /Users/iamtron/Documents/GitHub/HomerFindr/frontend && test -f src/components/ui/Card.jsx && test -f src/components/ui/Badge.jsx && test -f src/components/ui/Button.jsx && grep -q "HomerFindr" src/App.jsx && grep -q "brand-600" src/App.jsx && ! grep -q "HomeSearch" src/App.jsx && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "HomerFindr" frontend/src/App.jsx` returns the brand name in the nav
    - `grep "HomeSearch" frontend/src/App.jsx` returns NO matches (old name removed)
    - `grep "brand-600" frontend/src/App.jsx` shows the new green nav color
    - `grep "brand-700" frontend/src/App.jsx` shows the active state color
    - `grep "export function Card" frontend/src/components/ui/Card.jsx` confirms Card export
    - `grep "export function CardHeader" frontend/src/components/ui/Card.jsx` confirms CardHeader export
    - `grep "export function Badge" frontend/src/components/ui/Badge.jsx` confirms Badge export
    - `grep "export function Button" frontend/src/components/ui/Button.jsx` confirms Button export
    - `grep "cn" frontend/src/components/ui/Card.jsx` shows cn utility is used
  </acceptance_criteria>
  <done>Card/Badge/Button primitives exist with cn() usage, App.jsx shows "HomerFindr" with brand-600 green nav, all blue-600 references in nav replaced with brand-600</done>
</task>

</tasks>

<verification>
- `npx vite build` in frontend/ succeeds (no import errors, no missing deps)
- `grep -r "HomeSearch" frontend/src/` returns zero matches
- `grep -r "HomerFindr" frontend/src/App.jsx` returns the branded nav
- All three ui/ component files exist and export named functions
- `tailwind.config.js` contains brand color palette with 50-900 shades
</verification>

<success_criteria>
1. Tailwind config defines brand color palette (green 50-900) and Inter font family
2. cn() utility exists at frontend/src/lib/utils.js using clsx + tailwind-merge
3. Card, Badge, Button primitives exist in frontend/src/components/ui/ using shadcn/ui patterns
4. App.jsx nav shows "HomerFindr" (not "HomeSearch") with brand-600 green background
5. Frontend builds successfully with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui-redesign/03-02-SUMMARY.md`
</output>
