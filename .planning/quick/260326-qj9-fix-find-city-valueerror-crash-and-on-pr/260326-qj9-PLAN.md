# Quick Task 260326-qj9: Fix find_city ValueError crash and on_progress callback signature in web search

**Date:** 2026-03-26
**Status:** Executed

## Tasks

### Task 1: Fix find_city ValueError in search_locations
- **File:** homesearch/api/routes.py:241
- **Action:** Wrap `search.find_city(city_part, best_match=False)` in try/except ValueError, return `[]` on error
- **Why:** uszipcode raises ValueError for unrecognized city names (e.g. 'Abbott'), crashing the autocomplete endpoint

### Task 2: Fix on_progress callback signature mismatch
- **File:** homesearch/api/routes.py:71
- **Action:** Add `found: int = 0` parameter to the `on_progress` closure
- **Why:** search_service wraps all on_progress calls with 4 args (current, total, location, found), but the callback in routes.py only accepted 3, causing TypeError
